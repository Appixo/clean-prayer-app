/// API constants. Placeholder for when remote API is added.
/// Base URLs, timeouts, retry counts. Use --dart-define for secrets.
class ApiConstants {
  ApiConstants._();

  // static const String baseUrl = String.fromEnvironment('API_BASE_URL', defaultValue: 'https://api.example.com');
  // static const Duration connectTimeout = Duration(seconds: 10);
  // static const Duration receiveTimeout = Duration(seconds: 30);
}
